==================================
${roster.str_grf_name} ${utils.get_git_tag_or_version()}
==================================
Trains, for OpenTTD

${roster.str_grf_name} docs are published online:
  ${utils.get_docs_url()}

=======
GRF Name: ${roster.str_grf_name}

This version of ${roster.str_grf_name} is created using repository version ${utils.get_git_tag_or_version()}

Thanks to *all* who helped create ${roster.str_grf_name}.

